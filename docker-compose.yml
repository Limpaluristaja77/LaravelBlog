services:

  db:
    image: mariadb
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: example
      MARIADB_DATABASE: blog
    ports:
      - 33061:3306
    volumes:
      - blogdbdata:/var/lib/mysql
volumes:
  blogdbdata: